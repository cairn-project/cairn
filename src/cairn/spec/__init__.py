"""Spec package — holds the canonical work-unit JSON Schema and SPEC.md.

The schema is loaded via importlib.resources from this package (see
``cairn.validate``); these files are the authoritative interface artifacts.
"""
